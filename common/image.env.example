# Optional fallback for image builds without Helm values.
# Preferred: set image.fullRef in <demo>/helm/values.yaml
#
# Copy to image.env and customize (image.env is gitignored)
RMF_IMAGE=quay.io/your-org/openrmf-openshift-office-demo:certified
