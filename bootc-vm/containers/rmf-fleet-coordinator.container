# launch-fleet-coordinator.sh + fleet-coordinator.py confirmed identical
# between the image-baked copy and office/helm/files/ — no overlay needed.
[Unit]
Description=OpenRMF fleet coordinator (centralized task assignment)
After=rmf-simulation.service
Requires=rmf-simulation.service

[Container]
Image=quay.io/rhkp/openrmf-openshift-office-demo:nav2-sensors
ContainerName=rmf-fleet-coordinator
Network=host
Pull=never
GlobalArgs=--storage-opt=additionalimagestore=/usr/lib/bootc/storage
ShmSize=64m
Exec=/opt/rmf/scripts/launch-fleet-coordinator.sh
Environment=ZENOH_ROUTER_ENDPOINT=tcp/localhost:7447

[Service]
Restart=always

[Install]
WantedBy=multi-user.target
